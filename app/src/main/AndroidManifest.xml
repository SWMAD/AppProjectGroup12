<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dk.au.mad21spring.spacenewsapplication">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_spacenews_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_spacenews_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpaceNewsApplication">
        <activity android:name=".Activities.ArticleActivity"></activity>
        <activity android:name=".Activities.ReadLaterActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>